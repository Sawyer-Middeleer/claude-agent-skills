# Dot Claude

Custom agents and skills for Claude Code.

## Usage

Drop the `/skills` and `/agents` directories into your `.claude/` folder (personal or project-level).

The all skills & agents must all be copied into your `.claude/` directory as they depend on each other.